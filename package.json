{
  "name": "manhunt",
  "version": "1.0.0",
  "description": "Echtzeit GPS-Verfolgungsspiel Plattform",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "bun run --filter backend dev & bun run --filter frontend dev",
    "build": "bun run --filter backend build && bun run --filter frontend build",
    "test": "bun run --filter backend test && bun run --filter frontend test",
    "lint": "bun run --filter backend lint && bun run --filter frontend lint",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "keywords": [
    "manhunt",
    "gps-tracking",
    "real-time",
    "geofencing",
    "nestjs",
    "nextjs"
  ],
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
